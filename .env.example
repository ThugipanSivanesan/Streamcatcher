# Copy to .env and fill in real values. NEVER commit .env — it is gitignored.
# The stream URL may embed credentials (rtsp://user:pass@host); it is treated as
# a secret: wrapped in SecretStr and scrubbed from logs.
STREAMCATCHER_STREAM_URL=rtsp://user:password@camera.local:554/stream1
